<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flag Quiz</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/main.css">
</head>

<body id="app">
 <form class="container d-flex flex-column gap-3 justify-content-evenly bg-white p-3 mx-3 mx-lg-0" action="/submit" method="post">
  <div class="horizontal-container d-flex justify-content-between border-bottom pb-2">
    <h3 class="m-0 fs-3">
      Total Score:
      <span id="score">
        <%= locals.totalScore ? totalScore : "0" %>
      </span>
    </h3>
  </div>

  <h1 id="countryFlag" class="text-center">
    <%= question.flag %>
  </h1>
  <div class="w-100 answer-container text-center">
    <input type="text" class="text-center p-3 border-1 rounded" name="answer" id="userInput" placeholder="Enter the capital" autofocus autocomplete="off">
  </div>
  <div class="d-grid gap-2">
    <button type="submit" class="btn btn-lg btn-warning" name="action" value="submit">
      SUBMIT<% if(locals.wasCorrect){ %>
        <span class="checkmark">✔</span>
      <% } else if (locals.wasCorrect===false) { %>
        <span class="cross" id="error">✖</span>
      <% } %>
    </button>
    <button type="submit" class="btn btn-lg btn-outline-secondary fs-6" formaction="/skip">Skip</button>
  </div>
</form>

  <script>
    var wasCorrect = "<%= locals.wasCorrect %>";
    console.log(wasCorrect)
    if (wasCorrect === "false") {
      alert('Game over! Final best score: <%= locals.totalScore %>');
      document.getElementById("app").innerHTML = '<a href="/" class="btn btn-lg bg-white fs-4">Restart</a>'
    }


  </script>
</body>

</html>